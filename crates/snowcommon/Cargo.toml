[package]
name = "snowcommon"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Thin re-export shim for the merged snowdrive lib crate (plan §15.7)"
publish = false

[features]
log = ["snowdrive/log"]
defmt = ["snowdrive/defmt"]

[dependencies]
snowdrive = { path = "../../snowdrive", default-features = false }
