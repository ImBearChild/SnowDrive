[package]
name = "snowscsi-capi"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "C ABI bindings for snowscsi"

[lib]
crate-type = ["staticlib", "cdylib"]

[dependencies]
snowscsi = { path = "../snowscsi" }

[build-dependencies]
cbindgen = "0.27"
